%% Medallion layout as exposed in Smart City lake stage UI. Render: mmdc -i 04-medallion-data-lake.mmd -o 04-medallion-data-lake.png
flowchart TB
  subgraph ingest["Acquisition"]
    RAW["Bronze — raw/near-raw sensor events\n(immutable landing)"]
  end

  subgraph process["Curation"]
    SIL["Silver — validated, typed,\nnormalized timelines"]
    FEAT["Enriched streams\n(metrics, aggregates, QC flags)"]
  end

  subgraph serve["Consumption"]
    GOLD["Gold — analytics / ML-ready\npartitions & exports"]
    EXP["Exports & registry\nHF datasets, warehouses, downloads"]
  end

  RAW --> SIL --> FEAT --> GOLD --> EXP

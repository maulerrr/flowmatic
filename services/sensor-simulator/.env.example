# Sensor Simulator Service
PORT=8091
HOST=0.0.0.0

# Default city label embedded in generated payloads
DEFAULT_LOCATION=Astana

# Optional API key for simple auth (leave empty to disable)
API_KEY=

# Comma-separated allowed CORS origins (* = all)
CORS_ORIGINS=*
